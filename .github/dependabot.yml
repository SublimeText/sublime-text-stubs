version: 2

updates:
  # Actions are pinned to commit SHAs with a `# vX.Y.Z` comment;
  # Dependabot rewrites both the SHA and that comment in place.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: monthly
    groups:
      actions:
        patterns: ["*"]
